# divisionRaceProbabilities


## How to install Tailwind
`npm install -D tailwindcss` (or maybe just `npm install`?)
`npx tailwindcss init`

## How to generate new Tailwind CSS
`npx tailwindcss -i input.css -o output.css`